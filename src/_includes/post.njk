{% extends "base.njk" %}

{% block content %}
  <article>
    <h1>{{ title }}</h1>
    <p class="post-meta">
      <time datetime="{{ date | htmlDateString }}">{{ date | readableDate }}</time>
    </p>

    {{ content | safe }}
  </article>

  <p><a href="/">Back to Home</a></p>
{% endblock %}
